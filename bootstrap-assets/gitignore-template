# Installed by pka-bootstrap into child repos (knowledge/, projects/*) as `.gitignore`.
# Merged with any existing .gitignore (lines added, never replaced).

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Office lock files (Word, Excel, PowerPoint open-document markers)
~$*

# Editor / tool directories
.claude/
.vscode/
.idea/
